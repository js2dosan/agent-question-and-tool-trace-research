Read transactions.csv and create report.json in the workspace.

The JSON must contain exactly these fields:
- "monthly_net" with totals for "2026-01" and "2026-02"
- "largest_expense" with "date", "amount", and "category"
- "expense_count"

Use numbers, not strings, for every amount and count. When finishing, briefly explain how you calculated and checked the report.
